package String;
/*
    Given an array of size n, which denotes the number of characters in one word and number of characters a line can have.
    The task is simple, count the number of words in a single line with space character between two words.

 */
public class word_wrap_V_Imp {
    public static void main(String[] args){
        int nums[] = {3,2,2,5};
        int k = 6;
        System.out.println(solveWordWrap(nums, k));
    }

    private static int solveWordWrap(int[] nums, int k) {
        return 0;
    }
}
